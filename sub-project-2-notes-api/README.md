# Sub-project 2 — Planned

**Idea (default):** A notes app that saves data (via an API or localStorage) instead of losing it on refresh.

**Skills this adds on top of Project 1:**
- Working with an external API (fetch, async/await)
- Persisting data (localStorage, or a simple backend like Firebase/Supabase)
- Slightly more complex UI state (multiple notes, editing, deleting)

**Target time:** ~4-5 weeks at 5-10 hrs/week

**Status:** Not started — swap this idea out if Project 1 sparks a better one to build next.
